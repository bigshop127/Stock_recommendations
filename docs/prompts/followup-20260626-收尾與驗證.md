# 後續提示詞（2026-06-26 維運收尾）

> 開新對話，把以下整段貼上即可。任務有先後，①是主菜。

---

我要繼續處理 **財經APP / Stock_recommendations** 專案的維運收尾。請先讀這幾個記憶檔建立脈絡，再動手：
`vm-report-branch`、`old-twstockquant-repo`、`oracle-cloud-access`、`stock-review-web-plan`、`phase9-vm-deploy`。
（2026-06-26 已完成的修復記在 Obsidian `財經APP開發\維運紀錄.md`，背景看那份。）

## 環境速記（別重新推導）
- 三個 repo 共用同顆 Telegram bot：`Stock_recommendations`（本機 `C:\CC AI Agent`，VM 上跑，分支 **phase3-chips**）、`tw-stock-quant`（`C:\財經APP`，premarket/選股/盤中/health 雲端排程）、`PU-report`（舊浦惠摘要，**已 disable**）。
- VM：`ssh -i C:\Users\bigsh\.ssh\oracle_puhui.key ubuntu@140.238.48.197`，repo `/home/ubuntu/Stock_recommendations`，有免密 sudo。cron：老王 13:00、refresh 14:00、healthcheck */15（皆台北時間）。
- 已修好別再動：`refresh.sh`／`puhui_daily.cjs`／`sync_puhui_to_obsidian.ps1` 三支已改成走「目前分支」（commit `9283888`）；OAuth 失敗已不發 Telegram。
- `engine/.env` 是 untracked（reset 不會動到）。VM 的 `0 13 * * 1-5` 老王那行 cron 不要碰。
- 在 VM 上跑 `git reset --hard` 之類破壞性指令可能被 auto-mode 擋，會請我授權後再跑。

## ① 主任務：master ↔ phase3-chips 分叉收斂（Stock_recommendations）
現況：VM 每天把報告/訊號 push 到 **phase3-chips**；`master` 與 `phase3-chips` 已分叉（互含重複 commit、master 還缺部分每日報告）。目標＝收斂成單一事實來源、不遺失任何每日報告、不破壞 VM 的 push（它打 phase3-chips）。

請**先盤點再決策，不要盲目 merge**：
1. `git fetch origin`，比對 `origin/master` 與 `origin/phase3-chips`：各自 ahead/behind 幾個 commit、差異檔案（特別是 `reports/**`、`engine/`、`web/`、`deploy/`）、有沒有真衝突還是只是重複 commit。
2. 提出 1–2 個收斂方案給我選（例如：以 phase3-chips 為準併回 master、之後 VM 改推 master；或維持 phase3-chips 當主、把 master 對齊）。講清楚每個方案對「VM 每日 push 目標分支」「Obsidian 同步腳本分支」的後續影響。
3. 我選定後再執行；破壞性步驟先備份（backup branch / crontab）。收斂後同步更新：VM 上 checkout 的分支、`vm-report-branch` 記憶、`Oracle_VM_運維參考.md`、`維運紀錄.md`。

## ② 驗證下週一（06-29）排程實跑
平日才會觸發，週一跑完後一起驗：
- **premarket（tw-stock-quant，08:30）**：開盤前作戰計畫的「夜盤行情」要有三行真實數據——標普500（^GSPC）、費城半導體 SOX（^SOX）、台指期夜盤（TAIFEX），不再「資料不足」。
- **VM 13:00 老王**：Telegram 不再出現 OAuth `invalid_grant` 告警；Obsidian 當日報告有產出。
- **VM 14:00 refresh**：不再「git push 失敗」告警，phase3-chips 收到當日 `reports/`、`signals/`。
- **整天**：不再出現「浦惠投顧每日摘要」與 OAuth `deleted_client`（PU-report 已 disable）。
驗法：看 Telegram，或 `gh run list --repo <repo>`（時間 UTC，+8）／ssh 看 `puhui_daily.cron.log`、`cron_refresh.log`。有任何一項沒過就排查修掉。

## ③（低優先）舊記憶鏡像重建
`C:\obsidian\儲存庫\CC\memory（跨對話記憶）\記憶條目清單.md` 還停在早期 15 條 snake_case 名單，跟現行 `MEMORY.md` 已脫節。若有空，按 `MEMORY.md` 重建這份鏡像對齊；沒空就略過。

完成後照慣例：commit & push、同步 ROADMAP/Obsidian/記憶。
