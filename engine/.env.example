# 財經引擎金鑰（複製成 engine/.env 並填入真實值；.env 已 gitignored，不會進 repo）
#
#   cp engine/.env.example engine/.env
#
# 只有 FINMIND_TOKEN 是「建議必填」；其餘皆可選：
#   - 即時五檔預設走 TWSE MIS（官方、免金鑰），不需富果即可用。
#   - 只有要 /data/macro（美國總經）時才需要 FRED_API_KEY。

# ── FinMind（免費，需註冊）：歷史 OHLCV / 三大法人 / 融資券（回測主力）──
# 申請：https://finmindtrade.com/  → 會員中心取得 API token
FINMIND_TOKEN=

# ── 富果 Fugle（玉山）：即時報價 / 五檔 / 盤中分K（可選；live-only，當沖用）──
# 不填 → /data/book 自動走 TWSE MIS。要用富果再填，並把 BOOK_SOURCE 設 fugle 或 auto。
# 申請：https://developer.fugle.tw/  → 建立 Marketdata API Key
FUGLE_API_KEY=

# ── FRED 美國總經（可選，免費）：/data/macro 用 ──
# 申請：https://fred.stlouisfed.org/docs/api/api_key.html  （註冊即免費）
FRED_API_KEY=

# ── 選用：覆寫預設值 ──
# 即時五檔來源：auto（有富果 key 才用富果，否則 MIS）/ mis（強制官方免費）/ fugle（強制富果）
# BOOK_SOURCE=auto
# ENGINE_CACHE_DIR=data_cache
# HTTP_TIMEOUT=15
# HTTP_MAX_RETRY=3
