# Build artifacts
*.msi
node_modules/
.claude/
chat_history/

# Logs
*.log
data/puhui_*.log
data/oracle_*.log
data/server_*.log

# Pipeline 中間產物
data/puhui_urls_part_*.json
data/puhui_urls_v*.json
data/puhui_urls_api.json
data/puhui_urls_full.json
data/puhui_urls_public.json
data/puhui_urls_rss.json
data/puhui_page_debug.*
data/puhui_2026-*_raw.txt

# Secrets / cookies
.env
.env.local
.env.*.local
.env.bak
.env.*.bak
auth_url.txt
data/pressplay_cookies*.txt
data/pressplay_cookies*.db
data/temp_cookies.db
PressPlay_cookies.json
*pressplay_cookies*.json
data/pressplay_cookies.json
data/pressplay_cookies_b64.txt

# Debug
.playwright-mcp/
firebase-debug.log
firestore-debug.log

# 過時殘留（保險）
PowerShell-*.msi
HANDOVER_*.md

# === Added 2026-06-05 (S1 inventory) ===

# Inventory artifacts (per-stage cleanup reports)
INVENTORY_*.md
inventory_raw.txt

# IDE / tooling caches
.antigravitycli/

# Data: machine-generated caches and mutable state
data/puhui_cache.json
data/puhui_no_read_streak.json
data/finance_progress.json
data/parse_error_list.json
data/token_usage.json

# Data: large binaries / generated artifacts
data/all_books*.pdf
data/puhui_raw/
data/puhui_analysis/
data/patch_*.js

# Data: pipeline outputs (regenerable; Obsidian/VM hold canonical copies)
data/puhui_synthesis.md
data/puhui_urls_paginated_dated.json

# Data: oracle runtime artifacts (regenerated by oracle_capacity_grab.ps1)
data/oracle_metadata.json
data/oracle_shape_config.json
data/oracle_source_details.json

# === Added 2026-06-13 (Phase 1: Python engine) ===
# Python 引擎虛擬環境與快取（可重建，不進 repo）
engine/.venv/
__pycache__/
engine/**/__pycache__/
engine/.pytest_cache/
engine/**/*.pyc
data/engine_cache/

# === Added 2026-06-13 (Phase 2: 台股數據層) ===
# 引擎本地數據快取（parquet，可重建，不進 repo）
engine/data_cache/
# .env 已由上方 Secrets 區塊涵蓋（.env 任何目錄皆 ignore）

# === Added 2026-07-04 (opt10 再平衡 Email 告警) ===
# 個人持倉設定與告警狀態（含財務數字，不進 repo；範本 scripts/rebalance_holdings.example.json 才進版控）
data/rebalance_holdings.json
data/rebalance_alert_state.json

# === Added 2026-07-29 (期貨損益總覽 / 真實同步改 VM 執行) ===
# 期貨部位（含進場價與保證金餘額，同樣是個人財務數字，不進 repo）
data/futures_positions.json
# 真實同步的最近一次執行結果（含錯誤訊息與 log 尾巴，屬執行期狀態）
data/sync_holdings_status.json

# === Added 2026-07-30 (opt24/25 權益曲線＋保證金同步) ===
# 每日權益數快照（個人財務數字，由 scripts/futures_alert.cjs 寫入）
data/futures_equity_history.json
# 告警去重狀態（純執行期狀態）
data/futures_alert_state.json
# 外部 OpenAPI 的磁碟快取，隨時可重抓，不該進版控
data/taifex_margins.json
data/twse_holidays.json
# opt27 加權指數報價快取（gateway 自抓 TWSE MIS，同樣可重抓）
data/twse_taiex.json
# 保留下來的舊 log 樣本（見 ops-local-gateway-autostart.md §4.1b 的 pm2 當機迴圈）
data/_archive/

# === Added 2026-08-27 (opt36 已實現損益總覽：個股/ETF 已實現交易) ===
# 個股/ETF 已實現交易明細（含買賣價與數量，個人財務數字，不進 repo）
data/stock_realized_trades.json

# === Added 2026-08-28 (資產變化圖：淨資產快照) ===
# 銀行帳戶＋股市＋期貨的淨資產快照（含銀行餘額，個人財務數字，不進 repo）
data/networth_snapshots.json

# === Added 2026-08-29 (每月信用卡帳單自動記帳) ===
# 信用卡帳單金額/繳款日（個人財務數字，不進 repo）
data/monthly_bills.json
data/monthly_bills_sync_status.json
data/bills_fetch.log

# === Added 2026-08-30 (個股資料夾雲端持久化＋價格警示 Email 通知) ===
# 個人自訂資料夾清單與個股分類（個人使用資料，不進 repo）
data/stock_folders.json
# 個股價格警示設定與去重狀態、告警 log（含個人財務數字）
data/stock_price_alerts.json
data/stock_price_alert_state.json
data/stock_price_alert.log
