# Build artifacts
*.msi
node_modules/
.claude/
chat_history/

# Logs
*.log
data/puhui_*.log
data/oracle_*.log
data/server_*.log

# Pipeline 中間產物
data/puhui_urls_part_*.json
data/puhui_urls_v*.json
data/puhui_urls_api.json
data/puhui_urls_full.json
data/puhui_urls_public.json
data/puhui_urls_rss.json
data/puhui_page_debug.*
data/puhui_2026-*_raw.txt

# Secrets / cookies
.env
.env.local
.env.*.local
.env.bak
.env.*.bak
auth_url.txt
data/pressplay_cookies*.txt
data/pressplay_cookies*.db
data/temp_cookies.db
PressPlay_cookies.json
*pressplay_cookies*.json
data/pressplay_cookies.json
data/pressplay_cookies_b64.txt

# Debug
.playwright-mcp/
firebase-debug.log
firestore-debug.log

# 過時殘留（保險）
PowerShell-*.msi
HANDOVER_*.md

# === Added 2026-06-05 (S1 inventory) ===

# Inventory artifacts (per-stage cleanup reports)
INVENTORY_*.md
inventory_raw.txt

# IDE / tooling caches
.antigravitycli/

# Data: machine-generated caches and mutable state
data/puhui_cache.json
data/finance_progress.json
data/parse_error_list.json
data/token_usage.json

# Data: large binaries / generated artifacts
data/all_books*.pdf
data/puhui_raw/
data/puhui_analysis/
data/patch_*.js

# Data: pipeline outputs (regenerable; Obsidian/VM hold canonical copies)
data/puhui_synthesis.md
data/puhui_urls_paginated_dated.json

# Data: oracle runtime artifacts (regenerated by oracle_capacity_grab.ps1)
data/oracle_metadata.json
data/oracle_shape_config.json
data/oracle_source_details.json

# === Added 2026-06-13 (Phase 1: Python engine) ===
# Python 引擎虛擬環境與快取（可重建，不進 repo）
engine/.venv/
engine/**/__pycache__/
engine/.pytest_cache/
engine/**/*.pyc
data/engine_cache/

# === Added 2026-06-13 (Phase 2: 台股數據層) ===
# 引擎本地數據快取（parquet，可重建，不進 repo）
engine/data_cache/
# .env 已由上方 Secrets 區塊涵蓋（.env 任何目錄皆 ignore）

# === Added 2026-07-04 (opt10 再平衡 Email 告警) ===
# 個人持倉設定與告警狀態（含財務數字，不進 repo；範本 scripts/rebalance_holdings.example.json 才進版控）
data/rebalance_holdings.json
data/rebalance_alert_state.json

# === Added 2026-07-29 (期貨損益總覽 / 真實同步改 VM 執行) ===
# 期貨部位（含進場價與保證金餘額，同樣是個人財務數字，不進 repo）
data/futures_positions.json
# 真實同步的最近一次執行結果（含錯誤訊息與 log 尾巴，屬執行期狀態）
data/sync_holdings_status.json
